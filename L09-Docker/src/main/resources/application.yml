management:
  endpoints:
    enabled-by-default: false
  endpoint:
    health:
      enabled: true
      probes:
        enabled: true

database:
  url: "jdbc:postgresql://127.0.0.1:5430/demoDB"
  user: "usr"
  pwd: "pwd"